// Response times
export const low_response_time = ['p(95)<300']
export const medium_response_time = ['p(95)<1000']
export const high_response_time = ['p(95)<3000']

// Error rate
export const zero_error_rate = ['rate<=0.00']

// Base url
export const baseUrl = 'https://reqres.in'

// Project Id
export const projectId = 3637554

// Parameters used in requests
export let params = {
    headers: {
        "Content-Type": "application/json",
    }
};